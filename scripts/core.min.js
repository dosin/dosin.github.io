(function() {

  var pfx = location.pathname === "/" ? "" : "../";

  document.write(
    '<script src="' + pfx + 'scripts/jquery-2.1.4.min.js"></script>' +
    '<script src="' + pfx + 'scripts/common.js"></script>' +
    '<script src="' + pfx + 'scripts/wallpaper.js"></script>' +
    '<script src="' + pfx + 'scripts/index.js"></script>' +
    '<script src="' + pfx + 'scripts/blog.js"></script>' +
    '<script src="' + pfx + 'scripts/about.js"></script>'
  )

  /*var srcs = [
    pfx + 'scripts/jquery-2.1.4.min.js',
    pfx + 'scripts/common.js',
    pfx + 'scripts/wallpaper.js',
    pfx + 'scripts/index.js',
    pfx + 'scripts/blog.js',
    pfx + 'scripts/about.js'
  ]

  function loadScript(src) {
    var script = document.createElement('script');
    script.setAttribute('src', src);
    document.body.appendChild(script);
  }

  window.onload = function() {
    for (var i = srcs.length - 1; i >= 0; i--) {
      loadScript(srcs[i]);
    }
  }*/

})();